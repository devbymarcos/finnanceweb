import { useEffect, useState } from "react";

const SelectWallet = () => {
  const [value, setValue] = useState(null);
  return (
    <div>
      <select>
        <option></option>
        <option></option>
        <option></option>
      </select>
    </div>
  );
};

export default SelectWallet;
